# GitHub Pages demo

The repository contains a static product demo in `web_demo/`.

To publish it:

1. Open repository settings on GitHub.
2. Go to `Pages`.
3. Set source to `GitHub Actions`.
4. Run the `Pages` workflow manually.

GitHub does not allow this repository's default workflow token to enable Pages automatically, so the workflow is intentionally manual.

